interface AboutProps {
    children: React.ReactNode;
  }

const AboutPage = ({ children }: AboutProps) => {
    return(
                  <div>
                    {children}
                  </div>
    )
}
export default A